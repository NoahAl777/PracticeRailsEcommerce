<%if @post %>
<h1>'<%= @post.title %>' - Comments</h1>
<% else %>
<h1>All Comments</h1>
<% end %>

<ol>
<% @comments.each do |c| %>
  <li><%= link_to "#{c.content} - #{c.user.username}", comment_path(c) %></li>
<% end %>
</ol>