<h1>All Comments - "<%= @comments.first.post.title %>"</h1>

<ol>
<% @comments.each do |c| %>
  <li><%= link_to "#{c.content} - #{c.user.username}. #{c.post.title}", comment_path(c) %></li>
<% end %>
</ol>