<h1><%= @post.title %> - $<%= @post.price %></h1> 
<h3><%= @post.content %></h3>
<h4>Author: <%= @post.user.username %></h4>

<% if @post.accepted? %>
  <h4>Purchased by: <%= @post.buyer_user.username %></h4>
<% elsif @post.user_id = current_user%>
  <%= form_for(@post) do |f| %>
    <%= submit_tag "Edit" %>
  <% end %>
<% else %>
  <%= form_for(@post) do |f| %>
    <%= submit_tag "Buy this item" %>
  <% end %>
<% end %>

<h4><%= link_to "All Comments", comments_path %> - 
<%= link_to "Add Comment", new_comment_path %></h4>
<% @post.comments.each do |c| %>
  <li><%= link_to c.content, comment_path(c) %> - <%= c.user.username %></li>
<% end %>
