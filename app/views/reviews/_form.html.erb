<%= render partial: 'layouts/errors', locals: {object: @review} %>

<%= form_for(@review) do |c| %>
  <%= c.hidden_field :post_id %>
  <%= c.label :content %>
  <%= c.text_area :content %>
  <br><br>
  <%= c.submit "Submit" %>
<% end %>